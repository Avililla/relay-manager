# ===========================================
# Relay Manager - Variables de Entorno
# ===========================================
# Copia este archivo a .env.local y ajusta los valores

# -------------------------------------------
# Base de datos
# -------------------------------------------
DATABASE_URL="file:./prisma/dev.db"

# -------------------------------------------
# Autenticación (NextAuth)
# -------------------------------------------
# Genera un secreto con: openssl rand -base64 32
AUTH_SECRET="tu-secreto-super-seguro-aqui"

# URL base de la aplicación
AUTH_URL="http://localhost:3000"

# -------------------------------------------
# WebSocket para terminales serial
# -------------------------------------------
# Host del servidor WebSocket (IP o dominio)
# En desarrollo: dejar vacío para usar window.location.hostname
# En producción: IP o dominio del servidor
NEXT_PUBLIC_WS_HOST=

# Puerto del servidor WebSocket
# Por defecto: 3001
NEXT_PUBLIC_WS_PORT=3001

# Protocolo WebSocket
# ws: para conexiones sin SSL
# wss: para conexiones con SSL
# Dejar vacío para detectar automáticamente según HTTP/HTTPS
NEXT_PUBLIC_WS_PROTOCOL=

# ===========================================
# Ejemplo de configuración para producción:
# ===========================================
# NEXT_PUBLIC_WS_HOST=192.168.1.100
# NEXT_PUBLIC_WS_PORT=3001
# NEXT_PUBLIC_WS_PROTOCOL=ws:
